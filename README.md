1.Visual studio codeインストールしてください。 2.Visual studio codeの拡張機能にあるPythonをインストールしてください。 3.コマンドプロンプトに・py -m pip install pandas・py -m pip install PySimpleGUI・py -m pip install chardet・py -m pip install matplotlib・py -m pip install seaborn・py -m pip install scikit-learn とそれぞれ入力してください。
4.temperature.pyを開いてください。 5.csvファイルを読み込んでください。 6.グラフが表示された後は、そのまま閉じてください。再度GUIが表示されます。 7.入力値を記入して実行してください。
